package sbnz.integracija;

import sbnz.integracija.example.domain.User;
import sbnz.integracija.example.domain.AgeGroup;

rule "Age Group - Young Adult"
    when
        $i: Users(20 > age > 60)
    then
        $i.setAgeGroup(AgeGroup.ADULT);
        update($i);
end